apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: default
data:
  DB_HOST: "postgres"       # Sesuaikan jika pakai RDS
  DB_USER: "nops_admin"
  DB_NAME: "nops_db"
  AI_SERVICE_URL: "http://ai-service:5000/estimate-calorie"
  PORT: "8080"
